;; -*- scheme -*-

(define-module snow.gauche-bv-string-utils
  (export string-size
          string-byte-ref
          )

  (define string-size string-size)
  (define string-byte-ref string-byte-ref)
  )
